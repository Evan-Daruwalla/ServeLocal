# Secrets — never commit
.env
*.pem

# Database & backups (contain PII; regenerated/seeded at runtime)
db.json
db.json.*.tmp
db.sqlite
db.sqlite.*.tmp
backups/

# Dependencies (better-sqlite3, ADR-0013 — a documented ADR-0001 exception)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Claude Code / ruflo tooling & runtime state (personal, not project source)
.claude/
.mcp.json
.claude-flow/
.swarm/
